# PLANTILLA de referencia del Secret de auth. NO la apliques con un password
# en texto plano: en el flujo real Ansible crea este Secret a partir del vault.
# Mostrada solo para entender su forma.
---
apiVersion: v1
kind: Secret
metadata:
  name: valkey-auth
  namespace: data
type: Opaque
stringData:
  # En el despliegue real este valor viene de vault_valkey_password (cifrado).
  valkey-password: "REEMPLAZA_O_USA_ANSIBLE"
